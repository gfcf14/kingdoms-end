public enum EnemyKeyTypeCondition {
  custom,
  bounded,
  unbounded
}

public enum EnemyType {
  unspecified,
  ambusher,
  bewitcher,
  bomber,
  bouncer,
  champion,
  charger,
  exploder,
  idler,
  patroller,
  sentinel,
  shooter,
  teleporter
}
